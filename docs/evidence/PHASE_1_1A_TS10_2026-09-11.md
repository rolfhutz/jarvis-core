# Nachweis Schritt 1.1a, Rest — Kontext aus der Datenbank (TS-10)

**Stand:** 11. September 2026
**Repo-Basis:** `main` d12f218
**Entscheidungen:** TS10-E1, TS10-E2 (mit TS-26), TS10-E3, freigegeben von Rolf am 11.09.2026
**Status:** 12 Kern-Workflows geändert und veröffentlicht, Smoke-Test 116/116, statische Prüfung und Wiederherstellung bestanden. Keine Migration, keine neuen Credentials.

## 1. Ausgangsbefund

Kontextwerte standen in 12 Workflows im Code, nicht in 9 wie in der Übergabe 1.1a (Befund 7.2):

- `context_resolve`: echte Konfiguration. Bindungstabelle mit 6 Schlüsseln (davon 4 ohne Datenbankeintrag), Kontextliste, Schema- und Credential-Tabelle.
- 8 SUB und 3 ADP: Kontextliste bzw. Kontexttabelle nur zur Eingabeprüfung, doppelt zur Kontextweiche.
- Keine Kontextweiche hatte einen Ausweichausgang. Hätte man nur die Liste gelöscht, wäre ein unbekannter Kontext still verschwunden.
- Kein Smoke-Fall prüfte einen unbekannten Kontext in diesen 11 Workflows.

## 2. Umsetzung

| Paket | Inhalt |
|---|---|
| 1 | `context_resolve`: Knoten „Eingabe pruefen“, „Bindung lesen“ (`jv_privat_postgres`, TS10-E2), „Kontext bestimmen“. Grundcodes `input_invalid`, `binding_unknown`, `binding_disabled`, `context_not_active`, `binding_halted`, `hint_conflict`. `credential_ref` entfällt (niemand verwendet es). Smoke-Test: C05, E03/E04 auf `storage_gdrive.get_file@1.1.0` |
| 2 | 8 SUB: Kontextliste durch Formatprüfung ersetzt, Ausweichausgang der Kontextweiche auf neuen Abbruchknoten „Kontext ohne Weiche“ (`evidence_verify` auch Ablageweiche). `id_generate`: nur Formatprüfung. 3 ADP: Kontexttabelle entfernt, Ausweichausgang auf „Ergebnis ungueltige Eingabe“. Smoke-Test: Gegenproben X01–X11 |

Arbeitsannahmen (angekündigt in der Vorlage, umgesetzt):

- AT-1: `context_resolve` weist fail closed ab, auch bei deaktivierter oder angehaltener Bindung.
- AT-2: `credential_ref` entfällt, `db_schema` kommt aus `context_registry`.
- AT-3: Der Weg `context_not_active` wird erst in 1.1e mit der K-07-Probekonfiguration (U-4) belegt.
- AT-4: Die ortsbasierte Auflösung mit Mehrdeutigkeitsprüfung folgt nach V-4.
- Fallnummern X01–X11 statt K01–K11, weil K01–K07 im Smoke-Test schon vergeben sind.

## 3. Veröffentlichung (TS-23, TS10-E3)

Für alle 12 Workflows gilt `versionId` = `activeVersionId` (MCP `get_workflow_details`). Die Aufruferbeschränkung der Adapter ist erhalten.

| Workflow | aktive Version | Rücksetzpunkt |
|---|---|---|
| `context_resolve` | `1c733a77` | `2074ed36` |
| `id_generate` | `0affc2d8` | `37ba467b` |
| `idempotency_guard` | `6d4536fd` | `91967134` |
| `action_classify` | `9d7d5524` | `f8d8f0ed` |
| `tool_invoke` | `dfb840df` | `31adcd9e` |
| `evidence_verify` | `df9cd7b3` | `45a12287` |
| `fach_log_write` | `78529f70` | `4c519fd1` |
| `tech_log_write` | `a469f54b` | `9f71b1b5` |
| `error_handler` | `050410de` | `bc353fd1` |
| `tasks_internal` | `0d6df4a2` | `68593311` |
| `docstore_internal` | `e10dbb33` | `c2403962` |
| `casestore_internal` | `7b8c9854` | `7f29c91e` |
| `smoke_test` (Entwurf) | — | `3ba552f4` (vor Paket 1) |

## 4. Abnahme

| Nr. | Kriterium | Ergebnis | Beleg |
|---|---|---|---|
| T-1 | Keine Kontextwerte in Code-, Set-, If-Knoten; jede Kontextweiche mit verbundenem Ausweichausgang | bestanden: 13 Workflows, 46 Knoten, 11 Kontextweichen, 0 Befunde. Gegenprobe gegen d12f218: 23 Befunde in allen 12 Workflows. Selbsttest 6 Fälle | `tests/n8n/check_ts10.py` |
| T-2 | `context_resolve` aus der Datenbank | bestanden: C01/C02 aufgelöst, `db_schema` aus der Datenbank; C03 `binding_unknown`, C04 `hint_conflict`; C05 `privat_mail` → `binding_unknown` (stand nur in der alten Code-Tabelle) | Ausführung 22616 (Unterläufe 22617–22621), 22715 |
| T-3 | Fremder Kontext wird in jeder Weiche abgewiesen | bestanden: X01–X11; keine Schreibwirkung (Readbacks unverändert) | Ausführung 22715 |
| T-4 | Zugriff über `jv_privat_login` und Session Pooler auf `source_binding` | bestanden | Ausführung 22616 |
| T-5 | TS-23 je Paket | bestanden: 105/105, 116/116 | 22616, 22715 |
| T-6 | Export, Normalisierung, Wiederherstellung | bestanden (Abschnitt 5) | `tests/n8n/check_restore.py` |

## 5. Export und Wiederherstellung

- **Quelle:** 13 Downloads aus n8n Cloud (Rolf, 11.09.2026). Vier Dateien (`id_generate`, `idempotency_guard`, `fach_log_write`, `error_handler`) kamen zweimal nur als Chat-Inhalt, nicht als Datei an. Übernommen wurde der Chat-Inhalt. Knotenweiser Abgleich aller 13 Exporte gegen d12f218 und die Sollstände: Jede Abweichung ist genau die geplante Änderung, alle übrigen Knoten sind unverändert, die `versionId` stimmt mit der veröffentlichten Version überein.
- **Normalisiert** mit `tools/normalize_n8n_export.py` (Selbsttest 5 Prüfungen, 2 Gegenproben). `binaryMode` bleibt jetzt erhalten (TS-24), 13 Workflows mit `separate`.
- **Wiederherstellung:** frische lokale n8n-Instanz 2.35.7 (Node v22.22.2), leere SQLite-Datenbank, eigener Schlüssel, zwei Credential-Hüllen mit den festen IDs.
  - Import: `Successfully imported 14 workflows.`
  - Ausgabe von `check_restore.py`:

    ```
    {"workflows": 14, "knoten": 164, "credential_zuordnungen": 39, "subworkflow_verweise": 20, "aufruferregeln": 6, "binaermodus": 13}
    ERGEBNIS: BESTANDEN
    ```

- **Wirkung von TS-24:** Der Binärmodus übersteht jetzt die Wiederherstellung. Der Nachweis an einem Workflow, der tatsächlich Binärdaten verarbeitet, folgt in 1.1b/1.1d.

## 6. Nebenbefunde

1. `tools/normalize_n8n_export.py` schreibt fest `meta.jarvis_phase: "1.0"`, auch für Exporte aus 1.1. Das ist nur kosmetisch; ein Parameter `--phase` folgt bei der nächsten Änderung am Normalisierer.
2. `check_restore.py` prüft die Zuordnung Kontext-Credential nur bei Knoten, deren Name den Kontext nennt. Der neue Leseknoten „Bindung lesen“ ist davon nicht erfasst. Sein Credential ist durch TS10-E2 festgelegt, die Zuordnung ist geprüft (39 Zuordnungen).
3. Die Smoke-Läufe 22616 und 22715 haben weitere Vorgangsnummern verbraucht (TS-18).

## 7. Nicht getestet

- Weg `context_not_active`, weil kein inaktiver Kontext existiert. Folgt in 1.1e mit der K-07-Probekonfiguration.
- Ortsbasierte Auflösung und Mehrdeutigkeitsprüfung, weil sie nicht gebaut sind. Folgen nach V-4.
